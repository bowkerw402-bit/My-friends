---
kind: nightly-review
date: 2026-07-22
threads: 11
two_agent: True
---

# Day review, Wednesday 22 July 2026

_Swept 11 threads at 21:06. Claude assessed, Codex pressure tested._

## What we worked on
- Watched and filed ten YouTube videos into `My-friends/_reference/learning/` (newest first), lifting reusable craft into `~/.claude/toolbox/`; built a durable `adoption-backlog.md` referenced from the learning README, toolbox README, and memory (thread 2).
- Connected Canva and produced the first real on-brand BBS Instagram post end to end via the connector (thread 2).
- Rebuilt the procedural tree trunk at `APLakeside/tree.html` (`:3021/tree.html`) to kill ring seams and fix the base (thread 4).
- Rebuilt the BBS Evidence Desk services carousel as real 3D using three Meshy GLBs on an R3F turntable, live at `http://localhost:3000/services-carousel` (threads 9, 10).
- Removed spaced-hyphen separators from the Mission Control generator and fixed a phantom-run bug in `~/.claude/tools/MissionControlLib.psm1` (thread 8).
- Ran the scheduled routines: morning-report, social-drafts, trading-dashboard, and nightly-review (threads 1, 3, 5, 7, 11).

## Problems hit
- Carousel looked flat with parts sticking out; Will was ready to scrap the whole 3D idea (threads 9, 10).
- Root cause: another agent's uncommitted edit in `page.tsx` set `background-image: none`, so old CSS tiles and baked renders drew at once (thread 10).
- Tree trunk showed banding and bright ring lines: two faults in one, bark texture restarting at each cylinder join plus open segment rims catching light (thread 4).
- Tree base read as a floating open pipe (culling artifact from an uncapped, single-sided tube) (thread 4).
- Headless D3D renderer was flaky: OOM on browser launch and intermittent blank renders that looked like code throwing but were not (thread 4).
- DAILY.md said "1 run" while the console said "0 runs", with a phantom empty-field row (threads 5, 8).
- Social-drafts queue had wrong day-of-week labels (called 22 Jul "Tuesday"; it is Wednesday) and Tuesday's queue was never approved (thread 7).
- Canva brand kits need Pro and manual setup, so none existed for BBS (thread 2).

## Solutions found
- Carousel: retired the CSS prism, loaded the real Meshy GLBs through the repo's proven R3F patterns; rendered gold measured chroma 0.0776, inside the approved 0.070 to 0.092 band for the first time in the build. Marked SAFE TO SHOW (thread 9).
- Tree trunk: rebuilt each branch as one continuous tapered tube (not stacked cylinders) and mapped bark vertically to world height so it never resets; both seam types gone (thread 4).
- Tree base: folded the flare into the trunk tube, buried and capped the bottom, made bark double-sided (thread 4).
- Phantom run: identified a PowerShell AutomationNull vs `$null` bug in `Get-RunManifests`, where the null value bound to `-Manifests` became a one-element array containing `$null`; fixed so counts read 0 (thread 8).
- Dash fix: replaced every generator-inserted `" - "` with colons, brackets, or parentheses across `ConvertTo-BoardMarkdown` and `ConvertTo-DailyMarkdown` (16 edits); `Build-MissionControl.ps1` needed no change (thread 8).
- Canva: pulled real BBS values from `tools/design-bench/brands/bbs.json` and site tokens (navy `#061b3a`, ivory `#f4efe6`, champagne `#c9ae78`, pearl `#c7cbd1`; serif display, sans body) and generated, saved (`DAHQGehdjyw`), and exported a post to verify on-brand by eye (thread 2).
- Social-drafts: shifted the five ready posts to real schedulable weekday morning slots and corrected day labels (thread 7).

## Decisions made
- Faking 3D (CSS planes, photos of 3D on planes) is abandoned; real Meshy models on the R3F stack is the path forward (threads 9, 10).
- Toolbox defaults added: sub-agents scoped to tasks not roles; long-doc contradiction check for report QA; CLAUDE.md as four things; the Karpathy auto-research loop; the design refinement discipline (first draft is raw material, specify goal/layout/content/audience, big changes before small).
- Higgsfield parked; Canva adopted and proven live (thread 2).
- Em dashes inside LEDGER lesson text are out of scope; Will's ledger is not edited (thread 8).
- Trading video's $102k figure treated as one lucky options month; captured the method with the returns caveat and draft-only line, not the number (thread 2).

## Still unresolved
- Canva brand kit for BBS cannot be created without Canva Pro and manual setup, so assets are specified directly instead (thread 2).
- Higgsfield signup and Dispatch toggle remain Will's authorizations, still parked (thread 2).
- Open gate: govt-supplier-evidence-desk has 4 items in the approvals queue awaiting Will (thread 11).
- Open handoffs to Codex: outbound email ownership, and the AML Setup Pack pickup (thread 11).
- The uncommitted `page.tsx` edit came from another agent; coordination gap that caused it is not addressed.

## Worth keeping
- `_reference/learning/adoption-backlog.md`: the durable bridge from watched video to rollout, findable from any chat like the toolbox.
- The first live on-brand BBS post: [canva.com/d/7MJyZwAGSCtieCw](https://www.canva.com/d/7MJyZwAGSCtieCw), design id `DAHQGehdjyw`; proves the Canva connector pipeline works from the session.
- Real BBS brand values: navy `#061b3a`, ivory `#f4efe6`, champagne `#c9ae78` (used sparingly), pearl `#c7cbd1`; source `tools/design-bench/brands/bbs.json` and site tokens.
- The AutomationNull vs `$null` pattern in `MissionControlLib.psm1`: a returned "nothing" becomes a one-element `$null` array when bound to a parameter; wrap or guard before looping.
- The 3D lesson: flat cards, parts sticking out, and perspective disagreeing with rotation are all symptoms of faking 3D; use real geometry.
- Tree seam fix: continuous tapered tubes plus vertically world-mapped bark, capped double-sided base; noted in the tree lab memory.
- Carousel gold finally measured in band (chroma 0.0776, band 0.070 to 0.092); ground L 0.230 in band.

## Noise (safe to ignore)
- Nested `powershell` launch inside the PowerShell tool hits an internal error; call the script directly instead (thread 5).
- The `watch` skill "ffmpeg/yt-dlp missing" warning was a stale session PATH; both were installed.
- Headless D3D shader warnings and blank renders during the tree work were renderer flakiness, not code errors; several zoom-and-recheck cycles chased a non-bug (thread 4).
- Canva thumbnail URLs are login-gated and returned 3KB placeholders; peeking at them directly does not work, export a real PNG instead (thread 2).
- Console `Get-Content` mangled emoji and arrows (`â­`, `â€"`) as a display-encoding artifact, not file corruption; the Read tool renders UTF-8 correctly (thread 8).
- Thread 6 (16:48) is an earlier run of this same digest-assessment task; not new work.

---

## Codex challenge

## What Claude got wrong or overstated

> “Ran the scheduled routines: morning-report, social-drafts, trading-dashboard, and nightly-review.”

The record shows no trading dashboard execution or result, only its scheduled task prompt.

## What Claude missed that matters

The new Meshy carousel was declared “SAFE TO SHOW,” but the user never reviewed or accepted it. The thread ended with an interruption. Colour metrics do not validate composition, geometry, or usability.

## The single most important unresolved problem going into tomorrow

Get explicit user approval on the live 3D carousel. Until that happens, the repeated visual failure remains unresolved.
