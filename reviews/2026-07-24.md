---
kind: nightly-review
date: 2026-07-24
threads: 9
two_agent: True
---

# Day review, Friday 24 July 2026

_Swept 9 threads at 21:05. Claude assessed, Codex pressure tested._

## What we worked on
- BBS service pages (`Bowker Business Events` repo): built a new `/automation` route (`app/automation/page.tsx`) with the 3D `MonogramV2` hero, reusing site header/footer and brand tokens.
- A second, more deliberate rebuild of `/automation` as a "working-drawing sheet" under the "cinematically architectural" direction, with per-piece 3D/animation and full gate verification.
- A `deep-research` run on how surface texture is achieved in premium web / real-time 3D (navy-and-champagne).
- A read-only bounded review for Codex of `LogoAssembly.tsx`, `logo-assembly/page.tsx`, and `AtmosphericFx` in `MonogramV2.tsx`.
- Scheduled routines fired: morning-report, trading-dashboard, social-drafts, weekly-portfolio-pass, nightly-review.

## Problems hit
- First `/automation` build read as a generic premium-consultancy brochure and shrank the best asset (the 3D monogram) into a corner widget. Will: "what did i do to deserve a website so bad".
- CSS collisions with the 44KB `globals.css`: `.auto` forced `aspect-ratio: 1/1` (the empty-gap square), headings forced dark-on-dark, a `.cta` class collision drew a stray navy box.
- Repeated process failure: writing briefs and describing tools instead of running them. Will twice: "you dont look thorugh the obisidan vault and skills" and "i want you to actually use the fucking tools i give you".
- Two "defects" from the filmstrip (clipped headline, model reads as blobs) were false alarms caused by the narrow capture viewport and small frames.
- `resizetest.mjs` is hardcoded to the APLakeside intro at :3021 and ignores the URL passed to it.
- Session limit hit (resets 9pm Australia/Sydney), cutting off the trading-dashboard and social-drafts routines and the main design thread.
- Morning report fired a STALE ALARM: previous report ran 45.9h ago (yesterday's run missed).

## Solutions found
- Renamed colliding classes (`.auto` to `.svc-auto`) and stopped inheriting the forced heading colour; gap and legibility fixed.
- Ran the real gates: `predeliver.mjs` returned SAFE TO SHOW, exit 0 (ground L 0.257, accent chroma 0.0784, spread 0.0022, flat region 5.8%, clean console); `errcheck`, `eslint`, `rendered-html` tests (3/3), and `npm run build` all clean.
- `look3d` confirmed `automation.glb` is a real object (four tiles with embossed icons wired to a hub, 138k triangles), not clay, correcting the blob call.
- The pre-write read guard prevented overwriting `/websites` and `/evidence-tools` (finished bespoke pages using `SpinLogoShowcase` and `EvidenceDeskBuilder`); only `/automation` was genuinely missing.
- deep-research workflow completed and landed a cited texture report.

## Decisions made
- Direction chosen: "cinematically architectural", each service page as one sheet from a working-drawing set ("built properly" made literal).
- Stop building until Will steers; nothing committed or deployed, all left in a clean resumable state.
- Direction written to `design/service-pages-direction.md`, build record to `.ai/HANDOFF.md`, with `/automation` as the finished exemplar.
- Will called the second full build "absolute waste of credits", parked the work until the next day.

## Still unresolved
- Texture quality across the 3D work: `automation.glb` has no materials (generate-stage Meshy clay); all surface look is coming from the `BRAND_NAVY` override plus lighting, no normal or roughness maps. Will: "the texture on everythign u do is shit, fix that".
- Whether to keep the working-drawings direction, adjust a lever, or scrap it, pending Will's reaction to the one exemplar page.
- The other two routes (`/websites`, `/evidence-tools`) not yet reconciled with the new direction.
- Open gate: Government Supplier Evidence Desk has 4 items in its approvals queue.
- Two open handoffs to Codex (outbound email ownership; AML Setup Pack built).
- Morning-report staleness: check the routine so it does not miss twice.

## Worth keeping
- The globals.css collision pattern: generic class names (`.auto`, `.cta`, `.grid`) clash with the 44KB `globals.css`; namespace with a `svc-` prefix. `.auto` gets forced to a 1:1 square.
- The gate sequence that certifies a build: `predeliver.mjs` (house bands: ground L 0.20 to 0.27, accent chroma 0.070 to 0.092), then filmstrip, `look3d`, `errcheck`, `eslint`, `rendered-html`, `npm run build`.
- `case-study-bbs-hero-and-prism.md`: the biggest recorded BBS failure was "zero contrast" (navy on navy, separation 0.005 vs reference 0.650); fix is near-black top/bottom with a champagne glow band behind the subject. A brief that names navy as the page ground repeats the documented mistake.
- The recurring lesson, in Will's words: run the tools, do not write about them. Check `_templates/`, the design-funnel docs, and the skills list before producing a brief.
- `resizetest.mjs` is hardcoded to :3021 and ignores the passed URL.
- The texture research report from the deep-research run (cited, in the workflow output file).
- Trading state: paper book $10,134.60 (+1.35%), 0% drawdown, 2 open positions, 1 working order, no fills; FOMC 29 Jul is the nearest radar item.

## Noise (safe to ignore)
- The first `/automation` brochure build and its dark-on-dark and gap bugs: superseded by the working-drawings rebuild.
- The Claude Design copy-paste handoff brief: Will rejected it ("i dont give a hsit about the brief") in favour of using the tools directly.
- The two false-alarm filmstrip defects (clipped headline, blob model): both disproven by the tools.
- Orphaned background shell task-stop notifications (`bpgpmx7vn`, `by0qfnx3q`, `biui1w5k5`, `b850kf400`): teardown artifacts, no action.
- Duplicate session events appearing across thread 2 and thread 4: the same design session, not separate work.
- Social-drafts routine interrupted by Will before producing output.

---

## Codex challenge

### Wrong or overstated

> “`look3d` confirmed `automation.glb` is a real object … not clay”

It confirmed the geometry, not the surface quality. The raw record explicitly says the GLB has no materials and remains generate stage clay beneath a uniform navy override.

### Missed

The texture research completed but was neither reviewed nor applied. No material system, texture maps, or improved render passed visual approval. The bounded `LogoAssembly` review also ended without a recorded conclusion.

### Most important unresolved problem

The automation page still lacks an approved visual direction and credible surface treatment. Tomorrow must begin by applying the texture research to one isolated 3D asset, then getting Will’s approval before rebuilding any page.
