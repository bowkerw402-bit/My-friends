---
kind: nightly-review
date: 2026-07-18
threads: 12
two_agent: True
---

# Day review, Saturday 18 July 2026

_Swept 12 threads at 19:39. Claude assessed, Codex pressure tested._

## What we worked on
- Finished building the two-agent operating system (The Line funnel plus Mission Control), orchestrator v2, the vault rebuild, and the morning report (thread df396a2a, then a3c0b511).
- Pointed the finished system at real venture work: mapped BBS plus the Evidence Desk, produced an owner-assigned tracking board at `10-active/bbs-evidence-desk/STATUS.md`.
- Built the Evidence Desk call pack: Excel register, PowerPoint deck, Word guides, and a coded taxonomy (thread 0bb52ab3).
- APLakeside 3D hero work: gold material, daytime versus dusk mood, circular emblem motion, pixel calibration against the flat logo (thread 1df75a7d).
- BBS site: Build-a-Scoop configurator with cropped fruit photos, size-aware images, menu strip (thread e585d75b); copy cleanup, AI disclosure removal, hero angle and speed, compass showcase, and a Netlify deploy zip (thread f7b2a6c2).
- Added a 9pm nightly cleanup sweep and a Desktop transcript viewer.

## Problems hit
- Codex was blind: every multi-line prompt was truncated at the first newline when passed as a `codex.cmd` argument. Root cause of every shallow "what are we building?" reply.
- Codex CLI v0.142.4 is pinned to model `gpt-5.6-sol`, which the API rejects ("requires a newer version of Codex"), so image generation cannot be auto-dispatched.
- PII leak: `Add-RunIndexLine` still wrote the full task to `sessions.md` on PII-flagged runs (Codex found this, a real critical bug).
- Compass rendered as a black blob because the per-page export let page HTML and fingerprinted CSS drift, so pages loaded with no stylesheet.
- 3D gold went matte because the env map was welded to the dark dusk sky; daytime versions were oversaturated because hexes were eyeballed instead of measured.
- A Claude sub-instance whiffed in a live two-agent run, asking "what's the task?" for all three rounds while Codex carried it.
- Repeated PowerShell footguns (a self-recursing `Git` helper, invalid `[Position=1]`, inline `if` in a hashtable, backtick escapes, `-Force` re-import detaching functions) and OneDrive file locks during moves.

## Solutions found
- Fixed Codex truncation at the source: write the prompt to a UTF-8 sidecar file and hand Codex a one-line pointer. Verified by echo of tokens from lines 2 and 3.
- Made the Claude tool ledger genuinely verified via `--output-format stream-json` (real calls, not self-report); Codex's stay labelled declared.
- Fixed the PII index leak, run-collision IDs, masked git failures, and the "both must speak" honesty check.
- Replaced the fragile export with atomic `tools/export-site.sh` that verifies every referenced asset exists before declaring success.
- 3D: separated env map (material response, bright studio HDRI) from sky and lights (mood), then calibrated the emblem against the flat logo pixel by pixel (final scale 0.857).
- Netlify deploy assembled as the live homepage pulled byte for byte plus the three new pages, zipped to `C:\Users\bowke\Downloads\bbs-netlify-update.zip`.

## Decisions made
- Draft only, always. Neither agent sends, deploys, or spends.
- Canonical site is the live Netlify homepage. The unpublished 3D front section is shelved, but the NDIS page lives only there and must be rebuilt on the live site before that folder is retired.
- Lean run records by default (00-SUMMARY, transcript, tools, manifest; Brief/Plan/Output/Review only when real content exists).
- Every stated preference gets filed to `_reference/standards/` with a date. The no-dashes rule is enforced in four places (global instructions, standards, orchestrator prompt, memory).
- Client deliverables ship as Office files, gathered into `EVIDENCE DESK - Call Pack` on the Desktop.
- The deck is the client's sparse visual aid; all script words live in speaker notes only Will sees.
- Reference code scheme locked: `DRAWER-TYPE-SEQ` plus a stable Card ID plus five coded facets.
- Morning report moved to 07:30 wake-to-run; 9pm nightly sweep registered.

## Still unresolved
- Codex CLI model-pin blocker: image generation cannot be automated until Codex is updated (Will's call).
- The "exact wafer cone" regeneration is staged for Will's open Codex session, not built.
- NDIS page rebuild on the live site before the unpublished hero folder is put away.
- APLakeside hero feedback from the day's last message (lettering too basic for the luxury look, boat too basic, wants something between a yacht and a rowboat). The turn was interrupted before action.
- Domain cutover from Squarespace to Netlify still pending.
- One OneDrive-locked `APLakeside` duplicate folder was left in place (bundled, harmless).

## Worth keeping
- Codex truncation root cause and fix: multi-line prompts must go via a UTF-8 sidecar file, never as a command-line argument.
- Codex CLI is pinned to `gpt-5.6-sol` and rejected by the API. Auto-dispatch of Codex is blocked until it updates.
- Compass breakage was CSS drift from per-page export. `tools/export-site.sh` now verifies assets and prevents it.
- 3D rule: env map equals material response, sky and lights equal mood, keep them separate. Calibrated bands: brand gold `#b79b61` chroma 0.083, house ground lightness L 0.20 to 0.27.
- Netlify drag-drop replaces the whole site, so the homepage was pulled byte for byte. Deploy zip at `Downloads\bbs-netlify-update.zip`.
- The automations are Windows Scheduled Tasks (open Task Scheduler), not app routines, which is why Will could not find them in a routines folder.

## Noise (safe to ignore)
- Threads ea75299b, 1cde66f2, c209edb6, 404ff7eb, 2bb02a04, 0769dc01: repeated orchestrator init handshakes, two QUICKSTART drafts (the existing one needed no change), and a `sample.txt` listing test. No real output.
- The Codex truncation diagnostic hung with no timeout and was abandoned; the fix did not need it.
- A higher-resolution screenshot re-render made deck text smaller and corrupted rows, a dead end in the photo cropping loop.
- The "photo still shows dragonfruit after reset" scare was an async preload read on the same tick, not a real bug.
- The Netlify "nothing there" report was browser cache and a minimized checking viewport; the deploy was healthy.

---

## Codex challenge

### What Claude overstated

> “Added a 9pm nightly cleanup sweep and a Desktop transcript viewer.”

The viewer was verified. The record only shows the sweep being built, registered, and started once. It never records successful completion or a committed assessment.

### What Claude missed

The two agent health check still measures whether each agent produced text, not whether each completed the task. One Claude instance asked for the task three times, yet the run remained labelled `mode=two-agent`. That makes the success label materially misleading.

### Most important unresolved problem

Prove one complete unattended cycle: scheduled launch, substantive Claude and Codex participation, accurate mode classification, committed nightly assessment, and visible failure reporting. Until that passes, the operating system is built but not operationally trustworthy.
